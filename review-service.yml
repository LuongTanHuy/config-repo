server:
  port: 8090

spring:
  application:
    name: review-service

  datasource:
    url: jdbc:postgresql://localhost:5432/hv_apple
    username: huyd
    password: tanhuy9560
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 30MB

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

review:
  image:
    path: D:/DA/Image_HvApple/reviews

app:
  cors:
    allowed-origins: "http://localhost:3030"

logging:
  level:
    com.hvapple.review_service: DEBUG
